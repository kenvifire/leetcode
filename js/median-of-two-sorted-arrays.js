function binarySearch(ar, formIndex,toIndex, key) {
	var low = fromIndex;
	var hight = toInde x- 1;
	
	while (low <= high) {
		var mid = (n +m) >> 1;
		int midVal = a[mid]	
		
		if(midVal < key){
			low = mid + 1;
		} else if(midVal > key){
			high = mid - 1;
		}else {
			return mid;
		}
	}
	return -(low + 1);
}

/**
 * @param {number[]} nums1
 * @param {number[]} nums2
 * @return {number}
 */
var findMedianSortedArrays = function(nums1, nums2) {
 	   
};
